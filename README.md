1. var is a function scope, it can be declared again and the value can be changed.
let is a function scope, it cannot be declared again and the value can be changed.
var is a function scope, it cannot be declared again and the value can be changed.
2. Spread Operator (...) is used to spread the elements of an array or object.
3. map() works on each element and returns a new array.
filter() returns a new array with some elements according to the condition.

forEach() only runs a loop, does not return any array.

4. Using the arrow function (=>), you can write a small and simple function.
5. Using template literals, you can add variables or expressions inside the string through ${}.
